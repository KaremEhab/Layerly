enum LayerType {
  text,
  image,
  shape,
  icon,
  deviceMockup,
  group,
  componentInstance,
}

enum ShapeType {
  rectangle,
  roundedRectangle,
  circle,
  line,
  triangle,
  star,
  arrow,
  badge,
}

enum MockupDevice {
  iphone,
  android,
  macbook,
  browser,
}

enum ResizeHandle {
  topLeft,
  topCenter,
  topRight,
  centerRight,
  bottomRight,
  bottomCenter,
  bottomLeft,
  centerLeft,
  rotation,
}

enum BackgroundType {
  solid,
  gradient,
  image,
}
